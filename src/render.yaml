services:
  - type: web
    name: resilient-email-service
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npx tsc
    startCommand: node dist/server.js
    branch: main
    autoDeploy: true
